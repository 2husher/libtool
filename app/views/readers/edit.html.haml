- provide(:title, "Edit reader")

-if flash[:error]
  %div.error
    = flash[:error]

%div.lib_form
  %fieldset
    %legend
      Change reader's information
    = render partial: 'form', locals: { submit_lbl: "Update reader's data" }
